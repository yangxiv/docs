---
title: "H1"
description: "Description of your new file."
mode: "wide"
---

# H1

这是正文

## H2

这是正文

<Card title="New Card" icon="bolt">
  Insert content here
</Card>

<Tabs>
  <Tab title="Tab 1">
    文本本文本本文本本文本本文本本文本本文本本

    文本本文本本文本本文本本


  </Tab>

  <Tab title="Tab 2">

  </Tab>
</Tabs>

文本

### H3

这是正文

<Info>
  Callout Content
</Info>

<Check>
  Callout Content
</Check>

<Note>
  Callout Content
</Note>

<Tip>
  Callout Content
</Tip>

<Warning>
  Callout Content
</Warning>

<Accordion title="New Accordion" defaultOpen="false">
  /xiblab/xiblab/editor/main#new-accordion
</Accordion>

<CardGroup cols="2">
  <Card title="Card" icon="sparkles">
    1111
  </Card>

  <Card title="Card" icon="sparkles" />

  <Card title="Card" icon="sparkles" />
</CardGroup>

<Card title="New Card" icon="bolt">
  Insert content here
</Card>

| 122   | 121   |
| ----- | ----- |
| 23232 | 32323 |
| 2323  | 2323  |

<CodeGroup>
  ```javascript helloWorld.js
  console.log("Hello World");
  ```
</CodeGroup>

```
var_self= （typeofwindow ！=='undefined）
？ window// if in browser
：
（typeofWorkerGlobalScope！== 'undefined'& &selfinstanceof WorkerGlobalScope）
var_self= （typeofwindow ！== 'undefined”）
？ window// if in browser
（typeofWorkerGlobalScope！== 'undefined'&&selfinstanceof WorkerGlobalScope） （typeof WorkerGlobalScope ！== 'undefined' && e
ed'&&selfinstanceof WorkerGlobalScope）
var_self= （typeofwindow ！=='undefined"）
？ window// if in browser
：（
（typeof WorkerGlobalScope ！== 'undefined
```

<AccordionGroup>
  <Accordion title="New Accordion" defaultOpen="false">
    var\_self= （typeofwindow ！=='undefined）

    ？ window// if in browser

    ：

    ？ window// if in browser

    ：（

    （typeof WorkerGlobalScope ！== 'undefined
  </Accordion>

  <Accordion title="New Accordion" defaultOpen="false">
    （typeofWorkerGlobalScope！== 'undefined'& \&selfinstanceof WorkerGlobalScope）

    var\_self= （typeofwindow ！== 'undefined”）

    ？ window// if in browser

    （typeofWorkerGlobalScope！== 'undefined'&\&selfinstanceof WorkerGlobalScope） （typeof WorkerGlobalScope ！== 'undefined' && e

    ed'&&<u>selfinstanceof</u> WorkerGlobalScope）

    var\_self= （typeofwindow ！=='undefined"）
  </Accordion>
</AccordionGroup>

<Tabs>
  <Tab title="Tab 1">
    typeofwindow ！== 'undefined”）

    ？ window// if in browser

    （typeofWorkerGlobalScope！== 'undefined'&\&selfinstanceof WorkerGlobalScope） （typeof WorkerGlobalScope ！== 'undefined' && e

    ed'&&<u>selfinstanceof</u> WorkerGlobalScope）

    var\_self= （typeofwindow ！=='undefined"）
  </Tab>

  <Tab title="Tab 2">
    typeofwindow ！== 'undefined”）

    ？ window// if in browser

    （typeofWorkerGlobalScope！== 'undefined'&\&selfinstanceof WorkerGlobalScope） （typeof WorkerGlobalScope ！== 'undefined' && e

    ed'&&<u>selfinstanceof</u> WorkerGlobalScope）

    var\_self= （typeofwindow ！=='undefined"）
  </Tab>
</Tabs>